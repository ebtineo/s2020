{% extends 'alimentos/layout.html.twig' %}

{% block contenido %}

  <table>
      <tr>
          <th>alimento (por 100g) | </th>
          <th>energía (Kcal) | </th>
          <th>grasa (g) | </th>
          <th>VER XML | </th>
          <th>EDITAR </th>
      </tr>

      {% for alimento in alimentos %}
        <tr>
            <td><a href="{{ path('app_alimentos_ver', {'id': alimento.id}) }}">{{alimento.nombre}}</a></td>
            <td>{{ alimento.energia }}</td>
            <td>{{ alimento.grasatotal }}</td>
            <td><a href="{{ path('app_alimentos_verXML', {'id': alimento.id}) }}">Ver XML</a></td>
            <td><a href="{{ path('app_alimentos_edit', {'id': alimento.id})}}">Editar</a></td>
        </tr>
      {% endfor %}
      
  </table>
  <br><br><br>
  <a href="{{ path('app_alimentos_descargarPDF')}}">Descargar listado PDF</a>

{% endblock %}